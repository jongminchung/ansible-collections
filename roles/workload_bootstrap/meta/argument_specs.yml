---
argument_specs:
  configure:
    short_description: Configure workload bootstrap resources and reconcile root TLS trust.
    options: {}
  uninstall:
    short_description:
      Remove workload bootstrap Kubernetes resources and application namespace data.
    options: {}
  verify:
    short_description: Verify workload bootstrap trust resources.
    options: {}
