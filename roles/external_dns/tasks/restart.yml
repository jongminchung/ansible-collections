---
- name: Restart ExternalDNS
  ansible.builtin.include_role:
    name: dev_infra.deployment.k8s_deployment
    tasks_from: rollout_restart
  vars:
    dev_infra_deployment_restart_kind: Deployment
    dev_infra_deployment_restart_namespace: "{{ external_dns_namespace }}"
    dev_infra_deployment_restart_name: "{{ external_dns_release_name }}"

- name: Verify ExternalDNS after restart
  ansible.builtin.include_role:
    name: dev_infra.deployment.k8s_deployment
    tasks_from: rollout_status
  vars:
    dev_infra_deployment_rollout_namespace: "{{ external_dns_namespace }}"
    dev_infra_deployment_rollout_resource: "deployment/{{ external_dns_release_name }}"
    dev_infra_deployment_rollout_timeout_seconds: 180
